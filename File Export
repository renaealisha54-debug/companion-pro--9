import { saveAs } from 'file-saver';

const downloadCode = () => {
  const blob = new Blob([code], { type: "text/plain;charset=utf-8" });
  saveAs(blob, `project-file.${lang === 'javascript' ? 'js' : lang}`);
};
